<UserControl x:Class="Gu.Wpf.DataGrid2D.Demo.SelectedItemView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:dataGrid2D="http://gu.se/DataGrid2D"
             xmlns:local="clr-namespace:Gu.Wpf.DataGrid2D.Demo"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             d:DesignHeight="300"
             d:DesignWidth="300"
             mc:Ignorable="d">
    <UserControl.DataContext>
        <local:SelectedItemVm />
    </UserControl.DataContext>
    <Grid>
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>
            <DataGrid Grid.Column="0"
                      SelectionUnit="Cell"
                      dataGrid2D:ItemsSource.RowsSource="{Binding RowVms}"
                      dataGrid2D:ItemsSource.SelectedCellItem="{Binding SelectedItem}" />

            <ListBox Grid.Column="1"
                     ItemsSource="{Binding AllRowsItems}"
                     SelectedItem="{Binding SelectedItem}" />
        </Grid>
    </Grid>
</UserControl>
